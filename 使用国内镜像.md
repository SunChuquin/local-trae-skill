# 使用国内镜像下载 BGE 模型

## ✅ 已完成的修改

已在 `backend/app/services/embedding.py` 中添加国内镜像配置：

```python
# 设置使用国内镜像（HF-Mirror）
os.environ['HF_ENDPOINT'] = 'https://hf-mirror.com'
```

## 🚀 重新启动后端服务

### 步骤 1：停止当前运行的后端服务

如果在运行中，按 `Ctrl+C` 停止。

### 步骤 2：重新启动后端

```bash
cd backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

或者双击 `启动后端.bat`

### 步骤 3：观察日志

启动后应该看到以下输出：

```
2026-04-15 xx:xx:xx.xxx | INFO | app.services.embedding:_initialize_model:15 - 正在加载嵌入模型: BAAI/bge-large-zh-v1.5
2026-04-15 xx:xx:xx.xxx | INFO | app.services.embedding:_initialize_model:19 - 已配置使用国内镜像: https://hf-mirror.com
正在下载模型文件...
```

## 📥 镜像说明

### 使用的镜像
- **镜像地址**：https://hf-mirror.com
- **镜像类型**：HF-Mirror（国内镜像）
- **模型**：BAAI/bge-large-zh-v1.5
- **模型大小**：约 1.2GB

### 为什么使用 HF-Mirror？
1. **无需注册** - 不需要登录账号
2. **国内访问快** - 服务器在国内，下载速度快
3. **完全兼容** - 与 Hugging Face API 完全兼容

## ⚠️ 注意事项

### 1. 首次下载
首次运行时会下载模型文件到本地：
- Windows: `C:\Users\你的用户名\.cache\huggingface\hub\`
- Mac/Linux: `~/.cache/huggingface/hub/`

### 2. 后续运行
模型下载后，后续启动会直接使用本地缓存，无需重新下载。

### 3. 如果下载仍然失败

#### 检查网络
```bash
ping hf-mirror.com
```

#### 配置代理
如果需要代理：

**Windows CMD:**
```cmd
set HTTP_PROXY=http://127.0.0.1:你的代理端口
set HTTPS_PROXY=http://127.0.0.1:你的代理端口
```

**Windows PowerShell:**
```powershell
$env:HTTP_PROXY="http://127.0.0.1:你的代理端口"
$env:HTTPS_PROXY="http://127.0.0.1:你的代理端口"
```

**Mac/Linux:**
```bash
export HTTP_PROXY=http://127.0.0.1:你的代理端口
export HTTPS_PROXY=http://127.0.0.1:你的代理端口
```

## 🔍 验证安装

### 检查模型是否下载成功

启动后端后，访问 API 文档测试：

1. 打开浏览器访问：http://127.0.0.1:8000/docs
2. 找到 `GET /api/system/health` 接口
3. 点击 "Try it out" -> "Execute"
4. 应该返回：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "status": "healthy",
    "version": "1.0.0",
    "chroma_status": "connected",
    "embedding_model": "BAAI/bge-large-zh-v1.5",
    ...
  }
}
```

### 测试向量化功能

在浏览器中访问：http://127.0.0.1:8000/docs

找到 `POST /api/skill/test` 接口测试检索功能。

## 📝 其他可选镜像

如果 HF-Mirror 也不稳定，可以尝试：

### 1. ModelScope（需要注册）
```python
# 安装 modelscope
pip install modelscope

# 修改代码
from modelscope.hub.api import HubApi
api = HubApi()
api.login("your-token")  # 注册并获取 token
```

### 2. 手动下载模型
1. 访问 https://hf-mirror.com/BAAI/bge-large-zh-v1.5
2. 下载所有文件到本地目录
3. 修改代码指向本地路径

## 🎯 下一步

模型下载成功后，可以：

1. 启动前端服务
2. 创建知识库
3. 上传文档
4. 测试检索功能

---

**提示**：模型只需下载一次，后续启动会使用本地缓存。
