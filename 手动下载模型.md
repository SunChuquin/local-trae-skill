# 手动下载模型指南

## 📋 方案说明

由于网络问题无法直接从 Hugging Face 下载模型，我们提供两种解决方案：

### 方案 A：使用下载脚本（推荐）

#### 步骤 1：运行下载脚本

双击运行 `下载模型.bat`

脚本会自动从 **HF-Mirror**（国内镜像）下载所有必要的模型文件到：
```
C:\Users\hp\Documents\trae_projects\myskills\models\bge-large-zh-v1.5\
```

#### 步骤 2：等待下载完成

下载内容包括：
- ✅ config.json
- ✅ config_sentence_transformers.json
- ✅ model.safetensors（约 1.2GB，下载时间取决于网速）
- ✅ tokenizer.json
- ✅ tokenizer_config.json
- ✅ special_tokens_map.json
- ✅ vocab.txt
- ✅ modeling.py

#### 步骤 3：修改配置使用本地模型

编辑 `.env` 文件：

```env
# 注释掉在线模型
# EMBEDDING_MODEL=BAAI/bge-large-zh-v1.5

# 取消注释本地模型
EMBEDDING_MODEL=./models/bge-large-zh-v1.5
```

#### 步骤 4：重启后端服务

```bash
cd backend
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

---

### 方案 B：手动下载（备选）

如果脚本下载失败，可以手动下载：

#### 1. 访问模型页面

打开浏览器访问：
```
https://hf-mirror.com/BAAI/bge-large-zh-v1.5
```

#### 2. 下载所有文件

下载以下文件到 `models/bge-large-zh-v1.5/` 目录：

| 文件名 | 大小 | 必须 |
|--------|------|------|
| config.json | ~2KB | ✅ |
| config_sentence_transformers.json | ~1KB | ✅ |
| model.safetensors | ~1.2GB | ✅ |
| tokenizer.json | ~2MB | ✅ |
| tokenizer_config.json | ~2KB | ✅ |
| special_tokens_map.json | ~1KB | ✅ |
| vocab.txt | ~4MB | ✅ |
| tokenizer_config.json | ~2KB | ✅ |
| modeling.py | ~50KB | ✅ |

#### 3. 修改配置

编辑 `.env` 文件：
```env
EMBEDDING_MODEL=./models/bge-large-zh-v1.5
```

#### 4. 重启服务

---

## ⚠️ 重要提示

### 1. 模型文件完整性

下载完成后，`models/bge-large-zh-v1.5/` 目录应该包含以下文件：

```
models/
└── bge-large-zh-v1.5/
    ├── config.json
    ├── config_sentence_transformers.json
    ├── model.safetensors
    ├── tokenizer.json
    ├── tokenizer_config.json
    ├── special_tokens_map.json
    ├── vocab.txt
    └── modeling.py
```

### 2. 如果模型文件已存在

如果之前尝试下载过模型，可能已经在以下位置缓存了部分文件：

**Windows:**
```
C:\Users\hp\.cache\huggingface\hub\
```

**Mac/Linux:**
```
~/.cache/huggingface/hub/
```

可以尝试删除缓存目录中的部分下载，重新下载。

### 3. 网络问题

如果连 HF-Mirror 也无法访问：

#### 方法 1：使用代理
```cmd
set HTTP_PROXY=http://127.0.0.1:你的代理端口
set HTTPS_PROXY=http://127.0.0.1:你的代理端口
```

#### 方法 2：使用手机热点

有时公司/学校网络会限制下载，可以尝试手机热点。

#### 方法 3：使用其他设备下载

1. 在有网络的电脑上下载模型
2. 通过 U盘拷贝到本机
3. 放到 `models/bge-large-zh-v1.5/` 目录

---

## 🎯 下一步

模型下载并配置完成后：

1. ✅ 重启后端服务
2. ✅ 启动前端服务
3. ✅ 访问 http://localhost:3000
4. ✅ 创建知识库
5. ✅ 上传文档
6. ✅ 测试检索功能

---

## 📞 常见问题

### Q: 下载脚本失败？

**A:** 检查：
- 网络是否正常
- 是否有代理
- 磁盘空间是否充足（需要约 1.5GB）

### Q: 下载很慢？

**A:** 
- 使用代理加速
- 使用手机热点
- 或使用方案 B 手动下载

### Q: 重启后还是无法加载模型？

**A:** 检查：
- `.env` 配置是否正确
- 模型文件是否完整
- 路径是否正确（注意正斜杠/反斜杠）

### Q: 如何确认模型已成功加载？

**A:** 启动后端后，查看日志：

```
2026-04-15 xx:xx:xx.xxx | INFO | app.services.embedding:_initialize_model:20 - 正在加载嵌入模型: ./models/bge-large-zh-v1.5
2026-04-15 xx:xx:xx.xxx | INFO | app.services.embedding:_initialize_model:27 - 嵌入模型加载成功
```

如果看到 "嵌入模型加载成功"，说明模型已成功加载！

---

**提示**：模型只需下载一次，后续启动会直接使用本地文件。
