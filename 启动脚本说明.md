# 启动脚本使用说明

## 📁 脚本文件

### Windows 系统 (.bat)
- `启动后端.bat` - 仅启动后端服务
- `启动前端.bat` - 仅启动前端服务
- `一键启动.bat` - 同时启动前端和后端服务

### Mac/Linux 系统 (.sh)
- `启动后端.sh` - 仅启动后端服务
- `启动前端.sh` - 仅启动前端服务
- `一键启动.sh` - 同时启动前端和后端服务

## 🚀 快速启动

### Windows 系统

#### 方式一：一键启动（推荐）
1. 双击 `一键启动.bat`
2. 等待服务启动完成
3. 访问 http://localhost:3000

#### 方式二：分别启动
1. 双击 `启动后端.bat`
2. 等待后端启动（看到 "Uvicorn running on"）
3. 双击 `启动前端.bat`
4. 访问 http://localhost:3000

### Mac/Linux 系统

#### 方式一：一键启动（推荐）
```bash
chmod +x 一键启动.sh
./一键启动.sh
```

#### 方式二：分别启动
```bash
# 终端 1：启动后端
chmod +x 启动后端.sh
./启动后端.sh

# 终端 2：启动前端（新终端）
chmod +x 启动前端.sh
./启动前端.sh
```

## 📋 服务地址

启动后，访问以下地址：

- **前端界面**：http://localhost:3000
- **后端服务**：http://127.0.0.1:8000
- **API 文档**：http://127.0.0.1:8000/docs

## ⚠️ 注意事项

### 1. 首次运行
首次启动时需要：
- 下载 Python 依赖（如果未安装）
- 下载 BGE 嵌入模型（约 1.2GB）
- 下载 npm 依赖（如果未安装）

请确保网络连接正常。

### 2. 端口占用
如果端口被占用，会启动失败。请：

**Windows:**
```bash
# 查看端口占用
netstat -ano | findstr :8000
netstat -ano | findstr :3000

# 结束占用进程
taskkill /PID <进程ID> /F
```

**Mac/Linux:**
```bash
# 查看端口占用
lsof -i :8000
lsof -i :3000

# 结束占用进程
kill -9 <进程ID>
```

### 3. 启动顺序
建议按以下顺序启动：
1. 先启动后端（等待模型加载完成）
2. 再启动前端

### 4. 停止服务

**Windows:**
- 关闭对应的命令窗口
- 或按 Ctrl+C

**Mac/Linux:**
- 按 Ctrl+C
- 或使用 `kill` 命令

## 🔧 手动启动

如果不使用脚本，也可以手动启动：

### 后端
```bash
cd backend
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

### 前端
```bash
cd frontend
npm install
npm run dev
```

## 📞 常见问题

### Q: 双击脚本没反应？
**A:** 检查是否有杀毒软件拦截，或以管理员身份运行。

### Q: 提示"无法找到 Python"？
**A:** 确保已安装 Python 3.10+，并添加到系统 PATH。

### Q: 提示"无法找到 Node.js"？
**A:** 确保已安装 Node.js 18+，并添加到系统 PATH。

### Q: 模型下载失败？
**A:** 检查网络连接，或配置代理：
```bash
# Windows
set HTTP_PROXY=http://127.0.0.1:7890
set HTTPS_PROXY=http://127.0.0.1:7890

# Mac/Linux
export HTTP_PROXY=http://127.0.0.1:7890
export HTTPS_PROXY=http://127.0.0.1:7890
```

### Q: 前端无法连接后端？
**A:** 检查后端是否启动成功，确认 CORS 配置。

## 🎯 推荐启动流程

1. **双击** `一键启动.bat`（Windows）或 **运行** `./一键启动.sh`（Mac/Linux）
2. 等待两个服务窗口出现
3. 打开浏览器访问 http://localhost:3000
4. 开始使用系统

---

**提示**：保留启动脚本窗口，关闭即停止服务。
